<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
 <li> <% if task.completed %>
      <%= "done" %>
      <% else %>
      <%= "in work" %>
      <% end %>
  <%= link_to task.title, task_path(task) %> | <%= link_to "Delete", task_path(task), method: :delete, data: { confirm: "Are you sure?" } %></li>
  <% end %>
</ul>

<br>

<%= link_to "Ad a new task", new_task_path %>


